PyYAML>=6.0
python-dotenv>=1.0.0

# Database drivers (install as needed)
snowflake-connector-python>=3.6.0 ; extra == "snowflake"
google-cloud-bigquery>=3.11.0 ; extra == "bigquery"

# sqlite3 is built-in to Python, no dependencies needed
